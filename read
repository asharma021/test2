1. In your template (logical-rule.component.html)
Find the <mt-select> (or <lmn-select>) that you’re using to render your CSI list. It will look something like:

html
Copy
Edit
<mt-select
  [items]="TargetSystemCSIDropdownItems"
  [label]="'Target System CSI'"
  (valueChange)="onCsiSelected($event)">
</mt-select>
Change it to:

html
Copy
Edit
<mt-select
  [items]="TargetSystemCSIDropdownItems"
  [label]="'Target System CSI'"
  [groupBy]="csiGroupBy"            <!-- ← add this line -->
  (valueChange)="onCsiSelected($event)">
  <!-- optional: custom item template -->
  <ng-template let-item>
    {{ item }}
  </ng-template>
</mt-select>
2. In your component class (logical-rule.component.ts)
Import the grouping function type at the top:

ts
Copy
Edit
import { MtSelectGroupByFn } from '@citi-icg-172888/ng-icgds/select';
Declare the csiGroupBy function on your class:

ts
Copy
Edit
export class LogicalRuleComponent implements OnInit, OnDestroy {
  // … all your other fields …

  /**
   * If your items are simple strings like "FXCO TS : Trade Surveillance",
   * group key = the part before the first " : "
   */
  public csiGroupBy: MtSelectGroupByFn = (item: string) => {
    return item.split(' : ')[0].trim();
  };

  // … rest of your component …
}
If your dropdown items are objects (e.g. { TRGT_SYS_NM, CSI_ID, CSI_LABEL }), you can skip the function entirely and just do:

html
Copy
Edit
<mt-select
  [items]="TargetSystemCSIDropdownItems"
  [optionLabel]="'CSI_LABEL'"
  [optionValue]="'CSI_ID'"
  [groupBy]="'TRGT_SYS_NM'"
  [label]="'Target System CSI'">
</mt-select>
Bonus: Make sure you’ve imported the select module
In your YourFeatureModule (or wherever this component is declared), add:

ts
Copy
Edit
import { MtSelectModule } from '@citi-icg-172888/ng-icgds/select';

@NgModule({
  imports: [
    // …
    MtSelectModule,
  ],
})
export class YourFeatureModule { }
