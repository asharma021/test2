Changed FullName to displayName in notification templates to display the user-friendly identity name consistently in emails instead of the internal/full system name format.

Added <br/> tags to account detail fields in email templates to improve HTML email readability and ensure each attribute renders on a separate line in Outlook and other mail clients.
